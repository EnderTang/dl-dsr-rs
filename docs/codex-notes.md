# Codex Notes

Continuation target:

- Run `cargo test` and fix compile errors.
- Keep the current design.
- Make the 4-node Docker demo runnable.
- Make `dldsr-bench` produce JSON and Markdown summaries for DSR vs DL-DSR.

